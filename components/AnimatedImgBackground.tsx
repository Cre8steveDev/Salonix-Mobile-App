import { ImageBackground } from 'react-native';
import Animated from 'react-native-reanimated';

const AnimatedImgBackground = Animated.createAnimatedComponent(ImageBackground);

export default AnimatedImgBackground;
